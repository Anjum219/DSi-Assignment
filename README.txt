RUNNING INSTRUCTION:

To run the project from the command line, go to the dist folder and
type the following:

java -jar "Showroom_of_Vehicle.jar"



MY DESIGNING APPROACH

I created a project in Java to implement the vehicle showroom.
I followed the "Factory" design pattern where 'vehicle' is the interface
implemented by the three types of vehicles.